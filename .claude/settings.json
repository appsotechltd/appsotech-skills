{
  "$comment": "ui-ux-pro-max is vendored at .claude/skills/ui-ux-pro-max rather than installed as a plugin — see that directory's VENDORED.md. Declaring the plugin here as well would install seven skills on top of the one already on disk, six of them unused, and leave two copies to disagree."
}
